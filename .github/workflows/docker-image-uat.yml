name: Docker UAT Image CI

on:
  push:
    branches: [ uat ]
  pull_request:
    branches: [ uat ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile.prod --tag varungujarathi9/video-party-client:uat
    - name: Push to Docker Hub
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: varungujarathi9/video-party-client:uat
          tag_with_ref: true

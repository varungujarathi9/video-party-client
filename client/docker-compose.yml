version: '3.7'

services:

  client-sit:
    # container_name: video-party-client
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    environment: 
      - SERVER_TYPE="SIT"
      - SERVER_PORT="9002"
      - CLIENT_PORT="8002"
    image: varungujarathi9/video-party-client:sit
    ports:
      - 8002:3000

  client-uat:
    # container_name: video-party-client
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    environment: 
      - SERVER_TYPE="UAT"
      - SERVER_PORT="9001"
      - CLIENT_PORT="8001"
    image: varungujarathi9/video-party-client:uat
    ports:
      - 8001:80

  client-prod:
    # container_name: video-party-client
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    environment: 
      - SERVER_TYPE="Prod"
      - SERVER_PORT="1793"
      - CLIENT_PORT="80"
    image: varungujarathi9/video-party-client:latest
    ports:
      - 80:80    